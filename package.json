{
  "name": "assemble-cars-lambda",
  "version": "0.0.0",
  "description": "Assemble partial raw CAR files into complete CAR files",
  "scripts": {
    "format": "prettier -w src",
    "build": "bash scripts/build.sh",
    "init": "npm run build && DEBUG=assemble-cars:* node scripts/bin.js init",
    "test": "ava test/*.node.js",
    "deploy": "aws lambda update-function-code --cli-connect-timeout 15000 --region $AWS_REGION --function-name $LAMBDA --zip-file fileb://dist/lambda.zip",
    "deploy-l": "aws lambda update-function-code --cli-connect-timeout 15000 --region us-east-2 --function-name assemble-cars --zip-file fileb://dist/lambda.zip",
    "invoke": "aws lambda invoke --cli-binary-format raw-in-base64-out --region $AWS_REGION --function-name $LAMBDA --payload file://event.json --log-type Tail /dev/null | jq -r .LogResult | base64 -d"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nftstorage/assemble-cars-lambda.git"
  },
  "author": "Vasco Santos <santos.vasco10@gmail.com>",
  "license": "(Apache-2.0 OR MIT)",
  "dependencies": {
    "@ipld/car": "^3.2.4",
    "@ipld/dag-cbor": "^7.0.1",
    "@ipld/dag-pb": "^2.1.16",
    "aws-sdk": "^2.1084.0",
    "dotenv": "^10.0.0",
    "multiformats": "^9.6.4",
    "pino": "^7.6.2"
  },
  "devDependencies": {
    "@web-std/blob": "^3.0.1",
    "ava": "^3.15.0",
    "aws-sdk-mock": "^5.5.0",
    "carbites": "^1.0.6",
    "debug": "^4.3.3",
    "dotenv": "^10.0.0",
    "ipfs-car": "^0.6.1",
    "p-defer": "^3.0.0",
    "p-map": "^4.0.0",
    "pino-pretty": "^7.3.0",
    "prettier": "^2.4.1",
    "sade": "^1.8.1",
    "uint8arrays": "^3.0.0"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true
  },
  "bugs": {
    "url": "https://github.com/nftstorage/assemble-cars-lambda/issues"
  },
  "homepage": "https://github.com/nftstorage/assemble-cars-lambda#readme"
}
